#!/usr/bin/env bash
set -euo pipefail

# hash: {{ include "private_dot_config/sheldon/plugins.toml" | sha256sum }}

if command -v sheldon &>/dev/null; then
  echo "Sheldon plugins.toml changed, re-locking..."
  sheldon lock
fi
